`job parser`
==

```
usage: job_parsing.py [-h] -r RESOURCE -v VACANCY

optional arguments:
  -h, --help            show this help message and exit
  -r RESOURCE, --resource RESOURCE -> select between 'jooble' and 'tut'
  -v VACANCY, --vacancy VACANCY -> 'junior+python+django', for example

```

